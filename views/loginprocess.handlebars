<div>
    <h1>Login process</h1>
    <div>
        {{#if loggedIn}}
            <p>You are logged in!</p>
            <p>Welcome {{un}}</p>
        {{else}}
            <p>Sorry {{un}}, you are not logged in!</p>
            <a href="/login">Login page</a>
        {{/if}}
    </div>
</div>