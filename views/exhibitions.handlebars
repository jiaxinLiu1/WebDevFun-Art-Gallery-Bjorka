<div id="1">
  <h1>Our exhibitions</h1>
  <div>
    <ul>
      {{#each exhibitions}}
        <li class="exhibition-item">
          <h2>{{name}}</h2>
          <p>{{description}}</p>

        </li>
      {{/each}}
    </ul>
  </div>

</div>