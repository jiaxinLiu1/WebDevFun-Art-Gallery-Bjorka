<h1>{{exhibition.ename}}</h1>
<p><strong>Year:</strong> {{exhibition.eyear}}</p>
<p><strong>Location:</strong> {{exhibition.elocation}}</p>
<p><strong>Type:</strong> {{exhibition.etype}}</p>
<p>{{exhibition.edesc}}</p>
<img src="{{exhibition.eimgURL}}" alt="{{exhibition.ename}}" />

<hr />

{{#if session.isAdmin}}
  <form method="POST" action="/exhibitions/update/{{exhibition.id}}">
    <label>Name
      <input name="name" value="{{exhibition.name}}" required />
    </label><br />
    <label>Location
      <input name="location" value="{{exhibition.location}}" required />
    </label><br />
    <label>Year
      <input name="year" type="number" value="{{exhibition.year}}" required />
    </label><br />
    <label>Type
      <input name="type" value="{{exhibition.type}}" required />
    </label><br />
    <label>Description
      <input name="description" value="{{exhibition.description}}" required />
    </label><br />
    <label>Image URL
      <input name="image_url" value="{{exhibition.image_url}}" />
    </label><br />
    <button type="submit">Update</button>
  </form>
  <form method="POST" action="/exhibitions/delete/{{exhibition.id}}">
    <button type="submit">Delete</button>
  </form>
{{/if}}

<h2>Artists in this Exhibition</h2>
<ul>
  {{#each artists}}
    <li>
      <h3>{{name}}</h3>
      <p><strong>Nationality:</strong> {{nationality}}</p>
      <p><strong>Age:</strong> {{age}}</p>
      <p><strong>Famous Work:</strong> {{famous_work}}</p>
      <img src="{{image_url}}" alt="{{name}}" />
    </li>
  {{/each}}
</ul>

{{#unless artists}}
  <p>No artists found for this exhibition.</p>
{{/unless}}

<h2>Artworks in this Exhibition</h2>
<ul>
  {{#each artworks}}
    <li>
      <h3>{{title}}</h3>
      <p><strong>Medium:</strong> {{medium}}</p>
      <p>{{rewords}}</p>
    </li>
  {{/each}}
</ul>

{{#unless artworks}}
  <p>No artworks found for this exhibition.</p>
{{/unless}}

<a href="/exhibitions/{{exhibition.ex_id}}">{{exhibition.ename}}</a>